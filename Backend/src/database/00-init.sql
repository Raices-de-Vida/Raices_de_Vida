-- Script de inicialización para base de datos local
-- Este archivo se ejecuta automáticamente cuando se crea el contenedor de PostgreSQL

\echo 'Inicializando base de datos Proyecto1...'

-- Primero, las tablas deben ser creadas por Sequelize en el backend
-- Este script solo crea el esquema básico si no existe

\echo 'Base de datos lista para recibir conexiones'
